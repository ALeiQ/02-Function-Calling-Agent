"""FastAPI application entry point.

TODO(milestone 4): mount routes + static chat UI (mirrors project 01's layout).
"""

from __future__ import annotations

app = None  # placeholder until the API is built
