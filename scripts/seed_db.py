"""Create and populate the demo SQLite database (employees + orders).

TODO(milestone 2): build ``data/app.db`` with a couple of demo tables so the
``sql`` tool has something to query.
"""


def main() -> None:
    raise NotImplementedError("TODO(milestone 2): build and seed the demo DB.")